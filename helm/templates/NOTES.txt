Thank you for installing gRPC Studio!

Release:   {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

Backend:
  {{ include "grpc-studio.backendServiceName" . }}:{{ .Values.backend.port }}

Frontend:
  {{ include "grpc-studio.frontendServiceName" . }}:{{ .Values.frontend.port }}

{{- if .Values.ingress.enabled }}

Access gRPC Studio at:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}

{{- else }}

No Ingress is configured. Port-forward to access the UI:
  kubectl port-forward svc/{{ include "grpc-studio.frontendServiceName" . }} 8080:{{ .Values.frontend.port }} -n {{ .Release.Namespace }}

Then open http://localhost:8080 in your browser.

For production access, see the helm/examples/ directory:
  helm/examples/nginx-ingress/    - ingress-nginx + cert-manager
  helm/examples/istio/            - Istio Gateway + VirtualService

{{- end }}

Targets ({{ len .Values.connection.targets }}):
{{- range .Values.connection.targets }}
  - {{ .name }}: {{ .mode | default "plaintext" }} → {{ .host }}:{{ .port }}
{{- end }}

Switch between targets from the server selector in the UI.

Add or change targets by editing connection.targets in your values, e.g.:
  connection:
    targets:
      - name: PetStore
        host: petstore.default.svc.cluster.local
        port: 50051
        mode: plaintext
